# Telegram Downloader Bot (yt-dlp)
*Проект от LifeInvader Inc.*

![Демо-анимация бота](https://github.com/user-attachments/assets/9c4ac611-d3ef-42c5-adf1-bb57c3337a45)

Многофункциональный Telegram-бот на Python для скачивания видео и аудио с YouTube, VK, RuTube и тысяч других сайтов, поддерживаемых `yt-dlp`.

---

## 🚀 Ключевые возможности (Почему этот бот хорош)

Этот проект написан с упором на надежность, безопасность и удобство для пользователя.

### 1. Продвинутая загрузка
* Использует `yt-dlp` (преемник `youtube-dl`), что гарантирует поддержку максимального числа сайтов.
* **Умная обработка форматов:** Бот не просто качает "что есть", а анализирует форматы. Он находит оптимальные MP4-файлы, а если их нет — предлагает «склейку» (merge) отдельных потоков видео и аудио, чтобы получить наилучшее качество.
* **Конвертация в MP3:** Аудио-дорожки автоматически конвертируются в `.mp3` с помощью `FFmpeg`.

### 2. Надежность (Production-Ready)
* **Решена проблема "гонки файлов":** Для каждого скачивания создается уникальное имя файла (`user_id` + `timestamp`). Это позволяет десяткам пользователей скачивать файлы *одновременно*, не мешая друг другу.
* **Грамотная очистка:** Бот автоматически удаляет скачанный файл с сервера после отправки его пользователю (включая очистку мусорных файлов при ошибках).
* **Продвинутая обработка ошибок:** Бот не «упадет» от неверной ссылки. Он вежливо сообщит пользователю о проблеме (например, "TikTok блокирует IP" или "YouTube/VK недоступны").

### 3. Удобный UX (User Experience)
* **Интерактивное управление:** Используются `InlineKeyboard`-кнопки для выбора формата.
* **"Живой" отклик:** Бот редактирует свои сообщения («Ищу форматы...» -> «Выберите формат...»), а не спамит пользователя новыми.
* **Безопасность превыше всего:** Токен бота и другие ключи API вынесены в `config.py`, который добавлен в `.gitignore`. Ключи никогда не попадут в репозиторий.

---

## 🛠️ Стек технологий
* **Python 3.9+**
* **python-telegram-bot (v20+)** (асинхронная библиотека)
* **yt-dlp** (для скачивания)
* **FFmpeg** (для конвертации и склейки)

---

## 🚀 Как запустить
1.  Клонируйте репозиторий: `git clone https://www.nic.ru/info/blog/repository/`
2.  Создайте и активируйте виртуальное окружение: `python -m venv venv` и `source venv/bin/activate` (или `venv\Scripts\activate` на Windows)
3.  Установите зависимости: `pip install -r requirements.txt` (Тебе нужно будет создать этот файл!)
4.  Создайте файл `config.py` в корне проекта.
5.  Добавьте в `config.py` свой токен: `YT_BOT_TOKEN = "123456:ABC-..."`
6.  Запустите бота: `python bot.py`

# 🌦 AI Weather Bot (Telegram)

Умный Телеграм-бот, который не просто показывает температуру, а генерирует живой, остроумный прогноз погоды с помощью нейросети **Google Gemini**.

## 🔥 Особенности

* **Интеграция LLM (AI):** Использует **Google Gemini Pro** для анализа сырых метеоданных. Прогноз каждый раз уникален — бот может пошутить, дать совет по одежде или просто подбодрить.
* **Точные данные:** Получает погоду в реальном времени через **OpenWeatherMap API**.
* **Умный парсинг:** Понимает названия городов на разных языках.
* **Скорость:** Асинхронная архитектура на `aiogram 3.x` обеспечивает мгновенный ответ.

## 🛠 Технологический стек

* **Python 3.10+**
* **Aiogram 3.x** — лучший асинхронный фреймворк для Telegram.
* **Google Generative AI SDK** — интеграция с нейросетью Gemini.
* **Requests** — работа с API погоды.

## ⚙️ Как это работает (Логика)

1.  **Запрос:** Пользователь отправляет название города (например, "Москва").
2.  **API Погоды:** Бот делает запрос к OpenWeatherMap и получает JSON с данными (температура, влажность, ветер).
3.  **AI Анализ:** Бот формирует промпт для Gemini: *"Ты синоптик. Вот данные: {JSON}. Напиши короткий и смешной прогноз на русском"*.
4.  **Ответ:** Нейросеть возвращает готовый текст, который бот отправляет пользователю.

## 📦 Установка и запуск

1.  Клонируйте репозиторий:
    ```bash
    git clone [https://github.com/your-username/weather-bot.git](https://github.com/your-username/weather-bot.git)
    ```
2.  Установите зависимости:
    ```bash
    pip install -r requirements.txt
    ```
3.  Создайте файл `config.py` и добавьте ключи:
    ```python
    WEATHER_BOT_TOKEN = "ваш_тг_токен"
    WEATHER_API_KEY = "ваш_ключ_openweathermap"
    GEMINI_API_KEY = "ваш_ключ_gemini"
    ```
4.  Запустите бота:
    ```bash
    python weather_bot.py
    ```

## 📷 Пример работы

> **Пользователь:** Лондон
>
> **Бот:**
> 🇬🇧 **Лондон** 🌧
>
> Ну что, джентльмены, доставайте зонты! В Лондоне классика жанра — дождь и +12. Влажность такая, что у вашей собаки распрямятся кудри. Сидите дома, пейте чай и не жалуйтесь на жизнь! ☕️
